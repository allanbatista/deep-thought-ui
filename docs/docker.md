# Run Deep Thought with Docker

### Environment variables

- `VUE_APP_API_URL`: Backend API endpoint. Default: `http://localhost:3000`
- `VUE_APP_JWT_TOKEN_KEY`: Name key of JWT token in LocalStorage. Default `auth_token`
